{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Poison Apple&#39;s Portfolio",
  "author": "PoisonApple",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.3.4",
    "nuxt-stylus-resources-loader": "^2.0.0",
    "postcss-preset-env": "^6.5.0"
  },
  "config": {
    "nuxt": {
      "host": "0.0.0.0"
    }
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
